import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return <AppShellLoading variant="list" title="Audit logs" description="Loading audit entries..." />;
}
