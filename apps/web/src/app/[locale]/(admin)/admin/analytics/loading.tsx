import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return <AppShellLoading variant="stats" title="Analytics" description="Loading platform analytics..." />;
}
