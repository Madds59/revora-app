import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return <AppShellLoading variant="list" title="Notifications" description="Loading platform notifications..." />;
}
