import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return <AppShellLoading variant="form" title="AI search" description="Loading search..." />;
}
