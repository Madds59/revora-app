import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return <AppShellLoading variant="form" title="DTC decoder" description="Loading decoder..." />;
}
