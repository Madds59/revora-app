import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return (
    <AppShellLoading
      title="Vehicle"
      description="Loading the vehicle profile and related history..."
    />
  );
}
