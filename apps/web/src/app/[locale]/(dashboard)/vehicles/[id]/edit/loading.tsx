import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return (
    <AppShellLoading
      title="Edit vehicle"
      description="Loading the current vehicle and customer options..."
    />
  );
}
