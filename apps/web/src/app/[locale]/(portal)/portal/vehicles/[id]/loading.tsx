import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return <AppShellLoading variant="detail" title="Vehicle" description="Loading vehicle..." />;
}
