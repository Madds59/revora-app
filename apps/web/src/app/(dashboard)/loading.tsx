import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return (
    <AppShellLoading
      title="Dashboard"
      description="Loading business data and navigation..."
    />
  );
}
