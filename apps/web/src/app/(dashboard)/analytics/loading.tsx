import { AppShellLoading } from "@/components/app-shell-loading";

export default function Loading() {
  return (
    <AppShellLoading
      title="Analytics"
      description="Loading tenant metrics and summaries..."
    />
  );
}
